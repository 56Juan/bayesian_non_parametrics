name: model_ddp
channels:
  - conda-forge
  - defaults
dependencies:
  # Python base
  - python=3.12.2
  
  # Cientifico
  - numpy>=1.24
  - scipy>=1.10
  - pandas>=2.0
  - matplotlib>=3.7
  - seaborn>=0.12
  - xlrd>=2.0.1  
  
  # Notebooks
  - jupyter>=1.0
  - ipykernel>=6.0
  
  # Compilacion C++ - IMPORTANTE: orden correcto
  - pybind11>=2.10
  - cmake>=3.15
  - ninja
  - cxx-compiler
  - libpython
  
  # Utilidades
  - pyyaml>=6.0
  - tqdm>=4.65
  
  # Pip packages
  - pip>=23.0
  - pip:
      - scikit-learn>=1.3
      - xgboost>=2.0